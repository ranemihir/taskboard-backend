const projects = require('./projects');
const users = require('./users');
const projectRoles = require('./project_roles');
const tasks = require('./tasks');

module.exports = {
	projectRoles,
	projects,
	users,
	tasks
};